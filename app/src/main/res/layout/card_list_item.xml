<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/lytCardListItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:background="#000"
    android:padding="@dimen/activity_vertical_margin">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="@dimen/corner_radius_medium">

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="@dimen/activity_horizontal_margin">

            <CheckBox
                android:id="@+id/cbBuyItem"
                android:layout_width="32sp"
                android:layout_height="32sp"
                android:layout_margin="0sp"
                android:padding="0sp" />

            <TextView
                android:id="@+id/lblListItemName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignBaseline="@id/cbBuyItem"
                android:layout_toStartOf="@id/imgListItemImage"
                android:layout_toEndOf="@id/cbBuyItem"
                android:maxLines="3"
                android:textSize="@dimen/text_size_medium"
                android:textStyle="bold"
                tools:text="@tools:sample/full_names" />

            <ImageView
                android:id="@+id/imgListItemImage"
                android:layout_width="36sp"
                android:layout_height="36sp"
                android:layout_alignTop="@id/lblListItemName"
                android:layout_alignParentEnd="true"
                android:contentDescription="@string/list_item_image"
                android:scaleType="fitEnd"
                android:src="@drawable/ic_image"
                android:visibility="gone"
                tools:visibility="visible" />

            <ImageView
                android:id="@+id/imgListItemRepeating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/cbBuyItem"
                android:layout_alignStart="@id/cbBuyItem"
                android:layout_marginStart="4sp"
                android:contentDescription="@string/list_item_repeating"
                android:scaleType="fitEnd"
                android:src="@drawable/ic_loop"
                android:visibility="gone"
                tools:visibility="visible" />

            <TextView
                android:id="@+id/lblListItemPurchaseDate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/imgListItemRepeating"
                android:layout_marginTop="15sp"
                android:textSize="@dimen/text_size_small"
                android:textStyle="italic"
                android:visibility="gone"
                tools:text="Item purchased on 12/12/2020 by Gac"
                tools:visibility="visible" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>